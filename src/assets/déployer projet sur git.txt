cmd sur le dossier du projet

git init

git add .

git remote add origin (lien du git)

git branch -M main

git commit -m "Update README file"

git config --global user.email "jessyowen48@gmail.com"

git config --global user.name "Jessy48"

git commit -m "Update README file"

git branch my-feature

git checkout my-feature (switching branch)

git push -uf origin my-feature